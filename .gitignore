# Secrets — never commit the API key (explicit, so renames/new docs aren't silently ignored)
api_key.txt
/etc/secrets/

# Python
__pycache__/
*.py[cod]

# Node
node_modules/
server/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env/config
.env
.env.*

# Builds and caches
dist/
build/
.cache/
coverage/

# OS/IDE
.DS_Store
.vscode/

